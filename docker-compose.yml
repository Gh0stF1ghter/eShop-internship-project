version: '3.4'

services:
  identity.api:
    image: ${DOCKER_REGISTRY-}identityapi
    build:
      context: .
      dockerfile: Identity.API/Dockerfile

  catalogs.api:
    image: ${DOCKER_REGISTRY-}catalogsapi
    build:
      context: .
      dockerfile: Catalogs.API/Dockerfile
  identity.tests:
    image: ${DOCKER_REGISTRY-}identitytests
    build:
      context: .
      dockerfile: Identity.Tests/Dockerfile

